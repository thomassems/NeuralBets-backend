from setuptools import setup, find_packages

setup(
    name='my-shared-constants', 
    version='1.0.0',
    packages=find_packages(),
    description='Shared constants and utilities for project services.',
)